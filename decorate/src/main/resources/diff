适配器模式主要用在新老系统之间，保证系统接口之间的兼容性，而又不必去修改原有的接口。
装饰者主要是继承原有对象，通过包装增强原有对象的行为，满足开发的需求。
根本上来说，还是目的不同，前者主要是兼容原有对象，后者主要是增强、扩展对象。

装饰者模式和静态代理模式的区别：
    1、职责不同。
    2、静态代理不一定要满足is-a的关系。
    3、静态代理会做功能增强，同一个职责变得不一样。
    4、装饰器更多考虑的是扩展。





